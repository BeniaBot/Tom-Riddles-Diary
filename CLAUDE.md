# CLAUDE.md

הנחיות לעבודה על המאגר הזה.

## מה הפרויקט עושה

"היומן של תום רידל" (Tom Riddle's Diary) הוא **מתיחה**: צ'אט AI מזויף ומשחק
איקס־עיגול שאי אפשר לנצח, שרצים כולם בתוך מיקרוסופט וורד — באמצעות **תיקון
שגיאות אוטומטי (AutoCorrect) בלבד**. אין קוד שרץ בזמן ריצה, אין מאקרו, אין רשת.
כל "תשובה" היא רשומת AutoCorrect שוורד מחליף תוך כדי הקלדה.

> 🤫 הפואנטה היא שזה *לא* AI. אין להוסיף טקסט שמסגיר את המנגנון (AutoCorrect /
> וורד / "החלפה") לשום תגובה, לוח או הודעה שהצופים עשויים לראות.

## מבנה

```
generate_tomriddle.py       ← מקור האמת. מייצר את כל שאר הקבצים.
src/
  TomRiddle_Setup.bas       ← מודול VBA: התקנה (TomRiddle_Install)
  TomRiddle_Remove.bas      ← מודול VBA: הסרה (TomRiddle_Uninstall)
  TomRiddle_Diag.bas        ← מודול VBA: אבחון (TomRiddle_Diag)
easy-install/
  Install-TomRiddle.vbs     ← התקנה בלחיצה כפולה (בלי עורך VBA)
  Uninstall-TomRiddle.vbs   ← הסרה בלחיצה כפולה
docs/
  guide.md                  ← מדריך למפעיל (עברית)
  replacements.txt          ← רשימת כל ההחלפות — לעיני המפעיל בלבד
README.md                   ← עברית (עטוף ב-<div dir="rtl">)
README.en.md                ← אנגלית
```

## ⚠️ הכלל החשוב ביותר

**אין לערוך ידנית את `src/*.bas`, `easy-install/*.vbs` או `docs/replacements.txt`.**
כולם **מיוצרים אוטומטית**. הטקסט העברי בתוכם מקודד כקודי־תווים (ראה ChrW למטה),
כך שעריכה ידנית תשבור אותו או תיצור חוסר־סנכרון מול המחולל.

כל שינוי — תגובת שיחה, הודעה, שם, לוגיקת משחק — נעשה ב-`generate_tomriddle.py`
ואז מריצים אותו מחדש.

## איך מריצים את המחולל

```bash
python generate_tomriddle.py
```

הסקריפט כותב את הפלט ל**תיקייה הנוכחית**, לא ליעד הסופי. לכן אחרי ההרצה צריך
לפזר את הקבצים:

| פלט המחולל | יעד במאגר |
|---|---|
| `TomRiddle_Setup.bas` / `_Remove.bas` / `_Diag.bas` | `src/` |
| `Install-TomRiddle.vbs` / `Uninstall-TomRiddle.vbs` | `easy-install/` |
| `tomriddle_all_replacements.txt` | `docs/replacements.txt` (משנה שם) |

דרך נוחה: להריץ בתיקייה זמנית ולהעתיק משם, כדי לא ללכלך את שורש המאגר.

פלט תקין נראה כך:

```
game states: 1120 | chat: 23 | dropped: set()
  TomRiddle_Setup.bas       47613 bytes ASCII-OK
  TomRiddle_Remove.bas      46014 bytes ASCII-OK
  TomRiddle_Diag.bas          934 bytes ASCII-OK
```

`ASCII-OK` הוא בדיקת שפיות — המחולל קורא את הקבצים מחדש עם `encoding="ascii"`
וייכשל אם חדר תו לא־ASCII.

## מוסכמות קריטיות

### 1. הטריגר `תתת` — בעברית, ולא `ttt`

ל-AutoCorrect יש **רשימות נפרדות לכל שפה**. רשומות שנוספות דרך אוטומציה נוחתות
ברשימה של שפה אחת. טריגר לטיני כמו `ttt` שמוקלד בהקשר אנגלי נבדק מול הרשימה
*האנגלית* ולעולם לא יורה, בעוד טריגר עברי נבדק מול הרשימה העברית — שם הכל מותקן.

**לכן טריגר המשחק חייב להישאר עברי.** אותו היגיון חל על טריגרי השיחה (`צאט`).
אלה **מילות־הפעלה פונקציונליות, לא מיתוג** — אין לשנות אותן בשינוי שם/מיתוג;
שינוי כזה גם שובר את ההתקנה אצל כל מי שכבר התקין.

### 2. קידוד ChrW — המקור נשאר ASCII נקי

קבצי `.bas` ו-`.vbs` נשמרים כ-**ASCII בלבד**. כל טקסט עברי נכתב כרצף קודי־תווים
ומפוענח בזמן ריצה על ידי פונקציית `U()`:

```vba
AddPlain U("1492 1497 1497 32 1510 1488 1496"), U("1513 1500 1493 1501 ...")
```

זה עוקף בעיות קידוד של עורך ה-VBA ושל WScript. בפייתון: `codes()` ממיר מחרוזת
לקודים, ו-`vstr()` בוחר אוטומטית בין מחרוזת ASCII רגילה לבין עטיפת `U(...)`.
`clean()` מסננת תווי ZWJ/כיווניות ותווים מחוץ ל-BMP (ל-`ChrW` אין מענה עבורם).

### 3. מניעת לולאה

וורד **לא** יחיל החלפה שהתוצאה שלה מכילה את הטריגר עצמו. לכן שום טקסט של לוח
אינו חוזר על הקוד שלו, וכל הודעות הלוח (`תורך!`, `ניצחתי!`, `תיקו!`, `ניצחת!`)
נבחרו כך שאינן מכילות טריגר כלשהו. **כשמוסיפים תגובה חדשה — לוודא שהערך אינו
מכיל את הטריגר שלו.**

### 4. התנגשות תחיליות בשיחה

`_strip_prefix_conflicts()` מסירה אוטומטית כל טריגר שהוא הרחבה של טריגר אחר
(למשל אם קיים `צאט` וגם `צאט תודה`, השני יוסר) — כי הקצר יירה קודם. הערך
`dropped` בפלט מראה מה הוסר; אם הוספת תגובה והיא לא עובדת, לבדוק שם קודם.

### 5. רינדור הלוח

הלוח הוא רשת תווים עם מסגרת box-drawing (`BOX` במחולל), 8 שורות תצוגה:

```
┌───┬───┬───┐
│ X │ O │ 3 │
├───┼───┼───┤
│ 4 │ X │ 6 │
├───┼───┼───┤
│ 7 │ 8 │ O │
└───┴───┴───┘
תורך!  :)
```

- `ChrW(11)` — מעבר שורה **רך**, כדי שכל השורות יצטברו לרשת אחת בתוך רשומת
  AutoCorrect יחידה. **אין להסיר אף אחד מהם** — כולל האחרון, שאחרי הסטטוס,
  שהוא שמייצר את מעבר השורה האוטומטי בסוף הלוח.
- `ChrW(8234)` / `ChrW(8236)` — LRE/PDF, עוטפים כל שורה כדי שהספרות לא יתהפכו
  בתוך מסמך מימין־לשמאל.
- **אין ריפוד לפני המסגרת.** גרסה קודמת התחילה כל שורה ברווח אחרי ה-LRE, מה
  שהזיז את השורה הראשונה. המסגרת מתחילה מיד אחרי ה-LRE.

#### רוחבי תווים — נמדד, לא משוער

נמדד ב-`System.Drawing` על הגופנים המותקנים:

| פונט | ספרה | `─ │ ┼` | `✕` U+2715 | `❌` U+274C |
|---|---|---|---|---|
| **Consolas** | 11.00 | **11.00** ✅ | 22.00 ❌ | 20.00 ❌ |
| Courier New | 12.00 | 12.00 ✅ (קל בלבד) | 22.00 ❌ | 20.00 ❌ |
| **Calibri** (ברירת מחדל) | 10.14 | `┼`=22.00 ❌ | 22.00 ❌ | 20.00 ❌ |
| David / Arial / Times | לא עקבי ❌ | ❌ | ❌ | ❌ |

מסקנות מעשיות:
- הרשת מיושרת **רק בפונט רוחב־קבוע**. ב-Calibri היא נשברת — לתעד זאת למשתמש.
- **אין להשתמש באמוג'י או בסימני `✕`/`✖`/`◯`** — אין להם גליף בגופני המונוספייס
  והם נופלים ל-fallback ברוחב כפול. `X`, `O` ו-`○` (U+25CB) הם היחידים ברוחב 11.
- מסגרת **כבדה** (`┃ ━ ╋`) עובדת ב-Consolas אך לא ב-Courier New. נבחרה מסגרת קלה.

### 5א. מגבלות AutoCorrect שנבדקו מול Word 16.0

ממצאים שנמדדו ישירות — חשוב לפני שמציעים "לעצב" את הלוח:

- **אורך ערך מקסימלי = 255 תווים.** הלוח הנוכחי הוא 122 — יש מרווח, אבל לא הרבה.
- **`AutoCorrect.FormattedEntries` לא קיים** (שגיאה 438). אין API להחלפות מעוצבות.
- **`AutoCorrect.Entries.Add(name, Range)` משטח לטקסט** — הערך שנשמר הוא הטקסט
  בלבד ו-`RichText = False`. **אי אפשר להתקין טבלאות וורד אמיתיות דרך AutoCorrect
  בסקריפט**, גם אם בממשק הידני של וורד יש אפשרות "Formatted text".
- טבלאות אמיתיות אפשריות רק דרך **AutoText/Building Blocks** — אך אלה דורשים
  לחיצת **F3** ואינם נורים אוטומטית (נמדד: 0.176 שנ' לרשומה → ~3.3 דקות ל-1120,
  תבנית ~0.7MB). נדחה כדי לשמר את ההחלפה המיידית שהיא לב המתיחה.
- **`Selection.TypeText` דרך COM אינו מפעיל AutoCorrect** — אי אפשר לבדוק את
  ההחלפה מקצה־לקצה באוטומציה. לאימות: להוסיף רשומה, לקרוא `.Value` בחזרה
  (round-trip), ולהכניס את הערך למסמך ולספור `ComputeStatistics(1)`.

### 6. חלוקה לתתי־שגרות (`CHUNK = 140`)

ל-VBA יש מגבלת גודל לפרוצדורה יחידה. 1,120 מצבי המשחק מפוצלים ל-8 תתי־שגרות
(`InstallGame1`…`InstallGame8`, וכנגדן `RemoveGame1`…`RemoveGame8`). אם מספר
המצבים משתנה — מספר השגרות מתעדכן אוטומטית, אין לגעת בזה ידנית.

### 7. הסרה עם רשת ביטחון

`TomRiddle_Uninstall` מוחק את הרשומות הידועות, ואז סורק את כל רשימת ה-AutoCorrect
ומוחק כל רשומה שמתחילה בתחילית המשחק (`תתת`) — כדי לתפוס שאריות מגרסאות קודמות.
המספור בהודעת הסיום מבוסס על הפרש בין הספירה לפני ואחרי.

## סוף שורה (`.gitattributes`)

- `.bas` ו-`.vbs` → **CRLF** (וורד/WScript דורשים)
- `.md` ו-`.py` → **LF**

לא לעקוף את זה בהעתקת קבצים; לוודא ש-`git status` נקי אחרי הרצה מחדש.

## מיתוג

| הקשר | הצורה הנכונה |
|---|---|
| מזהי קוד, שמות קבצים | `TomRiddle_*`, `Install-TomRiddle.vbs` |
| תצוגה באנגלית | `Tom Riddle` (כותרת: `Tom Riddle's Diary`) |
| תצוגה בעברית | `תום רידל` (כותרת: `היומן של תום רידל`) |
| טריגרים למשתמש | `צאט`, `תתת` — **לא משנים** (ראה מוסכמה 1) |

שם המאגר ב-GitHub הוא `Tom-Riddles-Diary` — GitHub אינו מאפשר תווים עבריים
בשמות מאגרים.

## בדיקות ידניות

אין חבילת בדיקות אוטומטית. אחרי שינוי במחולל:

1. להריץ מחדש ולוודא `ASCII-OK` ואת ספירת המצבים (1120).
2. `git diff` — לוודא שהשינוי מוגבל למה שהתכוונת אליו.
3. בוורד: ייבוא `src/TomRiddle_Setup.bas` (**Import File, לא הדבקה** — השורה
   `Attribute VB_Name` תגרום ל-Syntax error) → F5 → `TomRiddle_Install`.
4. לבדוק `היי צאט` + רווח, ואז `בוא נשחק` + רווח, ואז `תתת5`.
5. לנקות עם `TomRiddle_Uninstall`.

אם המשחק לא נפתח — `TomRiddle_Diag` מציג את מספר הרשומות הכולל ואת ערך רשומת
המשחק.
